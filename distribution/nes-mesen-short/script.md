# Script — 55–60 seconds

**Hook card:** “A transformer on an NES. We checked the benchmark on a different emulator.”

**0:00–0:07**
A transformer running on a 1.79-megahertz NES sounds like a stunt. So we tried to reproduce the benchmark independently.

**0:07–0:16**
The original project measures a ternary transformer forward pass on the NES 6502 and verifies its generated tokens against a host reference.

**0:16–0:27**
We built a separate harness for Mesen 2.1.1, captured the CPU-cycle markers, and compared every generated byte.

**0:27–0:38**
Result: all nineteen tokens matched. Total: 21,227,718 CPU cycles. Mean: 1,117,248 cycles per token.

**0:38–0:48**
Mesen’s master-clock deltas were exactly twelve times the CPU-cycle deltas for every token.

**0:48–0:58**
And the mean differed from the published MAME result by zero cycles. Same model, different emulator, same measured number. The raw packet and harness are public.
